spring:
  application:
    name: demo
  datasource:
    url: jdbc:postgresql://localhost:5432/stock_manager?stringtype=unspecified
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
        hibernate:
          format_sql: true
